{
  "schema_version": 1,
  "schema_id": "burn-research.proof-provenance.v1",
  "purpose": "Separate caller claims from WASM-generated verification evidence and preserve verifier/reference provenance.",
  "authorities": {
    "legacy_recordProof": "caller-controlled legacy journal entry; not verifier authority",
    "caller_attestation": "explicit caller claim only",
    "vector_receipt": "WASM comparator authority over equality/tolerance relative to a caller-supplied reference",
    "graph_receipt": "WASM verifier authority using Burn CompiledGraph as the reference execution"
  },
  "surfaces": {
    "proofProvenanceCapabilities": {
      "mutation": "none",
      "returns": "this embedded contract"
    },
    "workspaceRecordAttestation": {
      "mutation": "AgentWorkspace attestation ledger only",
      "authority": "caller_attestation"
    },
    "workspaceVerifyVectorReceipt": {
      "mutation": "AgentWorkspace verifier-receipt ledger after successful comparison",
      "verifier": "mathVerifyVectors",
      "reference_authority": "caller_supplied"
    },
    "workspaceVerifyGraphReceipt": {
      "mutation": "AgentWorkspace verifier-receipt ledger after Burn-backed graph verification",
      "verifier": "CompiledGraph.verifyFlat",
      "reference_authority": "burn_compiled_graph"
    },
    "workspaceProofLedger": {
      "mutation": "none",
      "returns": "legacy proofs, attestations, and compact verifier receipts as separate collections"
    }
  },
  "receipt": {
    "schema_id": "burn-research.verifier-receipt.v1",
    "fields": [
      "receipt_id",
      "authority",
      "verifier",
      "reference_authority",
      "label",
      "fingerprint_algorithm",
      "reference_or_input_fingerprint",
      "candidate_fingerprint",
      "tolerances",
      "result"
    ],
    "graph_extra": [
      "program_identity",
      "program_identity_fingerprint",
      "reference_fingerprint",
      "mutable_state_in_program_identity=false"
    ]
  },
  "fingerprint": {
    "algorithm": "fnv1a64_noncryptographic",
    "purpose": "deterministic correlation and replay diagnostics",
    "security": "not collision-resistant and not a cryptographic signature"
  },
  "policy": {
    "malformed_inputs": "transport/validation error; no receipt is recorded",
    "numerical_mismatch": "valid verifier receipt with passed=false",
    "receipt_import": "forbidden; callers cannot inject a verifier receipt JSON into the internal ledger",
    "legacy_compatibility": "recordProof remains available but must be interpreted as caller-controlled legacy state"
  },
  "invariants": [
    "caller attestation cannot create verifier authority",
    "verifier receipts are created only by functions that execute the verifier",
    "graph receipt reference is produced by Burn CompiledGraph execution",
    "vector receipt reference remains explicitly caller-supplied",
    "failed numerical comparisons are recorded as failed receipts rather than transport errors",
    "invalid/non-finite/malformed inputs do not consume a verifier receipt id",
    "programIdentity remains authoritative for exact compiled graph structural identity",
    "graph receipt binds the observed numerical reference through reference_fingerprint because mutable weights/state are intentionally excluded from programIdentity"
  ]
}
