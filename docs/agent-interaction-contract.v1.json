{
  "schema_version": 1,
  "schema_id": "burn-research.agent-interaction.v1",
  "role": "projection_only",
  "state_ownership": "none",
  "purpose": "Provide a machine-readable planning view over existing agent workspace, graph builder, and registry state without introducing a new authority.",
  "authorities": {
    "intent_resolution": "ResolutionWorkflow and subject/review/authorization layers",
    "workspace": "AgentWorkspace metadata_and_control",
    "graph": "AgentGraphBuilder plan",
    "execution": "LayerRegistry",
    "numerics": "Burn reference machine",
    "verification": "explicit verifier entry points such as CompiledGraph.verifyFlat and mathVerifyVectors"
  },
  "invariants": [
    "interactionSnapshot and interactionValidActions are read-only projections",
    "no interaction object owns hidden mutable execution state",
    "availability never bypasses operation-specific validation",
    "unknown or parameter-specific conditions remain deferred to the authoritative call boundary",
    "escape hatches remain available and are not removed by the canonical interaction layer",
    "runtime evidence interpretation never chooses, ranks, or executes a response candidate",
    "AgentResponseIntent records an explicit caller/agent selection but never executes or authorizes the selected response",
    "response intent execution gate may project an authority handoff but never executes or authorizes that handoff",
    "response dispatch requirements describe machine-readable handoff inputs but bind no values and grant no authority",
    "response dispatch request may bind serializable caller payload but never fabricates runtime handles, executes an authority, or grants authorization"
  ],
  "availability_semantics": {
    "meaning": "available=true means at least one argument assignment can satisfy the known state predicates at snapshot time",
    "not_a_guarantee": [
      "a particular AgentLayerSpec is valid",
      "layout compatibility for unknown inputs",
      "runtime tensor shape compatibility",
      "external policy or authorization approval"
    ]
  },
  "phases": {
    "workspace_ready": "no graph steps and no reserved layer identity is awaiting specification",
    "spec_construction": "at least one workspace layer identity is reserved and the graph has no steps",
    "graph_ready": "the graph has at least one step and every referenced layer exists in LayerRegistry",
    "graph_incomplete": "the graph has at least one step but at least one referenced layer is absent from LayerRegistry"
  },
  "actions": {
    "reserveLayerId": {
      "class": "setup",
      "authority": "AgentWorkspace",
      "effect": "reserve metadata identity only; does not initialize execution state"
    },
    "reserveSlot": {
      "class": "contracted_direct",
      "authority": "AgentWorkspace"
    },
    "releaseSlot": {
      "class": "contracted_direct",
      "authority": "AgentWorkspace",
      "projection_candidate_policy": "only manually reserved slots are suggested; lower-level callers may still use the underlying method directly"
    },
    "workspaceInitUnary": {
      "class": "canonical",
      "authority": "workspace orchestration plus LayerRegistry",
      "parameter_specific_validation": true
    },
    "workspaceInitBinary": {
      "class": "canonical",
      "authority": "workspace orchestration plus LayerRegistry",
      "parameter_specific_validation": true
    },
    "workspaceCompile": {
      "class": "legacy_escape_hatch",
      "authority": "AgentGraphBuilder plus LayerRegistry",
      "projection_rule": "available when the builder has written outputs and all referenced layers exist in LayerRegistry",
      "subject_bound_policy": "does not create subject-program binding; a bound-subject graph receipt will fail unless the exact identity was already bound canonically"
    },
    "workspaceBindRuntimeSubject": {
      "class": "optional_semantic_context",
      "authority": "ResolutionRuntimeBridge host projection -> AgentWorkspace metadata",
      "availability": "workspace unbound and canonical runtime layer/slot reservation has not started",
      "effect": "bind exact intent/approval/subject/authorization-policy tuple; no LayerRegistry or graph mutation",
      "lifecycle": "exact repeat is idempotent; different rebind is rejected"
    },
    "workspaceCompileForRuntimeSubject": {
      "class": "canonical_subject_bound",
      "authority": "AgentGraphBuilder + LayerRegistry compile, then AgentWorkspace exact programIdentity binding",
      "availability": "runtime subject bound and normal compile candidates exist",
      "success": "returns CompiledGraph and records exact programIdentity idempotently",
      "failure": "controlled error with no program-binding mutation; builder/registry remain reusable",
      "requires": [
        "runtime_subject_bound",
        "workspace_builder_slot_domain_match",
        "registry_complete_for_builder",
        "runtime_program_binding_admissible"
      ]
    }
  },
  "conditional_rejoin_actions": {
    "workspaceWireUnary": "requires an externally initialized layer whose validated init identity exactly matches the supplied AgentLayerSpec",
    "workspaceWireBinary": "requires an externally initialized layer whose validated init identity exactly matches the supplied AgentLayerSpec"
  },
  "escape_hatches": [
    "AgentLayerSpec",
    "AgentGraphBuilder",
    "LayerRegistry",
    "raw_protocol"
  ],
  "surfaces": {
    "interactionCapabilities": {
      "mutation": "none",
      "returns": "capability JSON"
    },
    "interactionValidActions": {
      "mutation": "none",
      "arguments": [
        "AgentWorkspace",
        "AgentGraphBuilder",
        "LayerRegistry"
      ],
      "returns": "action projection JSON"
    },
    "interactionSnapshot": {
      "mutation": "none",
      "arguments": [
        "AgentWorkspace",
        "AgentGraphBuilder",
        "LayerRegistry"
      ],
      "returns": "point-in-time planning projection JSON"
    }
  },
  "input_contract": {
    "capability": "inputContractCapabilities",
    "binding": "workspaceBindInputContract",
    "clearing": "workspaceClearInputContract",
    "inspection": "workspaceInputContract",
    "compatibility": "inputContractCompatibility",
    "policy": "optional; unbound and unknown defer unless shape/layout proves incompatibility"
  },
  "input_port": {
    "capability": "inputPortCapabilities",
    "binding": "workspaceBindInputPortMetadata",
    "clearing": "workspaceClearInputPortMetadata",
    "inspection": "workspaceInputPortMetadata",
    "policy": "optional semantic role/provenance metadata for external slot 0; no execution authority",
    "consumer_contract": {
      "capability": "inputPortConsumerCapabilities",
      "spec": "InputPortConsumerSpec",
      "compatibility": "inputPortConsumerCompatibility",
      "policy": "read-only advisory preflight; consumer requirements are caller-declared, operation choice remains agent authority, and compatibility never authorizes execution"
    },
    "routing_projection": {
      "capability": "inputPortRoutingCapabilities",
      "valid_actions_overlay": "interactionValidActionsForInputConsumer",
      "edge_projection": "inputPortConsumerEdgeCompatibility",
      "policy": "semantic routing is annotation-only; the full canonical interactionValidActions envelope is preserved, candidates remain visible, and graph-step semantics are never inferred from numerical layer type"
    },
    "edge_binding": {
      "capability": "inputPortEdgeBindingCapabilities",
      "bind": "bindInputPortConsumerEdge",
      "inspection": "inputPortConsumerEdgeBinding",
      "semantic_identity": "semanticGraphIdentity",
      "policy": "agent-selected consumer semantics persist on exact topology-resolved external graph edges as immutable provenance; compatibility is recorded as advisory evidence rather than enforced, and binding never changes Burn executable plan identity or authorizes execution"
    },
    "semantic_lifecycle": {
      "capability": "semanticLifecycleCapabilities",
      "spec": "SemanticTransitionSpec",
      "bind": "bindSemanticLifecycleTransition",
      "inspection": "semanticLifecycleTransition",
      "projection": "semanticLifecycleProjection",
      "identity": "semanticLifecycleIdentity",
      "policy": "agent-declared role transitions form immutable provenance chains across graph steps; external lineage is anchored to persistent input edge binding and internal lineage to prior transition fingerprints; no numerical semantics or action authority are introduced"
    }
  },
  "proof_provenance": {
    "capability": "proofProvenanceCapabilities",
    "attestation": "workspaceRecordAttestation",
    "vector_receipt": "workspaceVerifyVectorReceipt",
    "graph_receipt": "workspaceVerifyGraphReceipt",
    "ledger": "workspaceProofLedger",
    "policy": "legacy recordProof, caller attestation, verifier receipts, and semantic proof context remain distinct authority classes; semantic context enriches graph proof provenance without changing Burn programIdentity",
    "semantic_execution_context": "semanticExecutionContext",
    "semantic_graph_receipt": "workspaceVerifySemanticGraphReceipt"
  },
  "resolution_runtime_bridge": {
    "capability": "resolutionRuntimeBridgeCapabilities",
    "native_projection": "RuntimeSubjectProjection::from_authorized",
    "runtime_binding": "workspaceBindRuntimeSubject",
    "inspection": "workspaceRuntimeSubject",
    "policy": "bridge transports already-approved/authorized semantic identity; it never chooses runtime actions or grants authority",
    "lifecycle": "bind before runtime planning state is reserved; binding is immutable for the workspace lifetime",
    "program_binding": "workspaceCompileForRuntimeSubject + workspaceRuntimeProgramBinding"
  },
  "runtime_resolution_evidence": {
    "capability": "runtimeResolutionEvidenceCapabilities",
    "native_inbox": "ResolutionEvidenceInbox",
    "accepts": [
      "structured AgentFault fields",
      "graph verifier receipt fields",
      "semantic graph verifier execution-context provenance",
      "MathProgram verifier receipt fields",
      "DirectMath verifier receipt fields",
      "vector verifier receipt fields"
    ],
    "policy": "evidence is classified against the exact Resolution revision + runtime subject; semantic graph proof context is preserved as observation-only provenance; no automatic ResolutionDiagnostic or state transition"
  },
  "math_interaction": {
    "capability": "mathInteractionCapabilities",
    "catalog": "mathOperationCatalog",
    "contract_schema": "burn-research.math-interaction.v1",
    "role": "read-only unified math vocabulary and direct/program binding projection",
    "authority": "none beyond catalog binding; operation choice remains agent authority and numerical semantics remain in existing math contracts",
    "valid_operations": "mathValidOperations",
    "valid_operations_role": "candidate projection only; selection, ranking, recommendation, execution, and mutation remain outside this surface",
    "binding_plan": "mathPlanBinding",
    "binding_plan_role": "explicit agent-selected direct|program binding projection; no dispatch, execution, builder mutation, or generation selection",
    "math_proof": "mathProofCapabilities",
    "math_proof_role": "MathProgram proof plus verifier-owned direct-vs-MathProgramV9 equivalence proof; direct verifier caller supplies neither numerical candidate nor reference",
    "direct_math_verifier": {
      "bind": "workspaceBindRuntimeDirectMathOperation",
      "unary": "workspaceVerifyDirectMath1Receipt",
      "binary": "workspaceVerifyDirectMath2Receipt",
      "candidate_authority": "burn_direct_math",
      "reference_authority": "burn_math_program",
      "selection_authority": "agent remains responsible for choosing the canonical operation; verifier only checks the chosen call"
    }
  },
  "runtime_evidence_interpretation": {
    "contract": "burn-research.runtime-evidence-interpretation.v1",
    "native_projection": "interpret_recorded_evidence",
    "role": "candidate projection over already-recorded exact-subject runtime evidence",
    "response_candidates": [
      "ignore",
      "reverify",
      "request_information",
      "propose_revision"
    ],
    "selection_authority": "caller/agent",
    "ranking": "none",
    "mutation": "none",
    "escape_hatch": "existing Resolution review/revision APIs remain independently callable"
  },
  "agent_response_intent": {
    "contract": "burn-research.agent-response-intent.v1",
    "create": "create_agent_response_intent",
    "preflight": "preflight_response_intent",
    "role": "explicit caller/agent selection encoded as immutable nonexecuting value object",
    "selection_authority": "caller/agent",
    "execution_authority": "none",
    "mutation": "none",
    "binds": [
      "recorded evidence fingerprint",
      "entry index",
      "selected response action",
      "candidate target boundary",
      "candidate handoff"
    ],
    "escape_hatch": "independent Resolution review/revision APIs remain callable outside this evidence-derived intent path"
  },
  "response_intent_execution_gate": {
    "contract": "burn-research.response-intent-execution-gate.v1",
    "project": "response_intent_execution_gate",
    "role": "validate a ResponseIntent and project the authority/operation handoff without executing it",
    "dispatchable": "means route is structurally valid against current inbox evidence and ResponseIntent preflight",
    "execution_authorized": false,
    "mutation": "none",
    "authorities": {
      "ignore": "none/no-op",
      "reverify": "runtime verifier",
      "request_information": "external resolution review",
      "propose_revision": "ResolutionRevisionChain"
    }
  },
  "response_dispatch_requirements": {
    "contract": "burn-research.response-dispatch-requirements.v1",
    "project": "response_dispatch_requirements",
    "role": "translate a dispatchable execution-gate route into machine-readable input requirements for the existing destination authority",
    "value_binding": "none",
    "execution_authorized": false,
    "mutation": "none",
    "identity": "requirements_fingerprint correlates the gate dispatch fingerprint with its ordered requirement profile and is not an authorization token"
  },
  "response_dispatch_request": {
    "contract": "burn-research.response-dispatch-request.v1",
    "bindability": "response_dispatch_request_bindability",
    "preflight": "preflight_response_dispatch_request",
    "serializable_factories": [
      "create_ignore_dispatch_request",
      "create_information_dispatch_request",
      "create_revision_dispatch_request"
    ],
    "reverify_policy": "runtime_handle_binding_deferred; graph/program/tensor handles are never represented by opaque caller strings",
    "execution_authorized": false,
    "mutation": "none"
  }
}
