name: Python Nonlinear ES Hyperparameter Sensitivity Research

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  nonlinear-es-hyperparameter-sensitivity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install research tooling
        run: python3 -m pip install --disable-pip-version-check "maturin==1.13.3" "cffi>=1.17,<2"

      - name: Run nonlinear ES hyperparameter sensitivity research
        run: python3 scripts/research_python_nonlinear_es_hyperparameter_sensitivity.py

      - name: Upload nonlinear ES hyperparameter sensitivity report
        uses: actions/upload-artifact@v4
        with:
          name: python-nonlinear-es-hyperparameter-sensitivity-report
          path: python-nonlinear-es-hyperparameter-sensitivity-report.json
          if-no-files-found: error
